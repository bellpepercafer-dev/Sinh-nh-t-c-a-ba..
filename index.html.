<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chúc Mừng Sinh Nhật Ba Yêu!</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #fff;
            text-align: center;
            overflow-x: hidden;
        }
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .main-avatar {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            border: 5px solid #ffd700;
            box-shadow: 0 0 25px rgba(255, 215, 0, 0.7);
            object-fit: cover;
            margin-bottom: 20px;
        }
        .title { font-size: 2.2rem; color: #ffd700; margin-bottom: 15px; }
        .hint { font-size: 1.1rem; color: #e0e0e0; animation: bounce 1.5s infinite; }
        .album-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .photo-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 50px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            opacity: 0;
            transform: translateY(40px);
            transition: all 0.8s ease-out;
        }
        .photo-card.active {
            opacity: 1;
            transform: translateY(0);
        }
        .photo-card img {
            width: 100%;
            max-height: 350px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 15px;
        }
        .photo-caption {
            font-size: 1.1rem;
            line-height: 1.6;
            color: #fff;
            text-align: left;
        }
        .final-wish {
            background: linear-gradient(135deg, rgba(255, 215, 0, 0.2), rgba(255, 118, 117, 0.2));
            border: 2px solid #ffd700;
        }
        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
    </style>
</head>
<body>

    <audio id="bg-music" loop>
        <source src="nhac.mp3" type="audio/mpeg">
    </audio>

    <div class="hero">
        <img src="anh1.jpg" alt="Mặt Ba" class="main-avatar">
        <h1 class="title">Chúc Mừng Sinh Nhật Ba! 🎂</h1>
        <p class="hint">👇 Vuốt xuống để xem lại những khoảnh khắc tuyệt vời 👇</p>
    </div>

    <div class="album-container">
        <div class="photo-card">
            <img src="anh1.jpg" alt="Khoảnh khắc 1">
            <p class="photo-caption">📸 <b>Kỷ niệm 1:</b> Chúc Ba luôn tràn đầy năng lượng và nụ cười rạng rỡ như thế này!</p>
        </div>

        <div class="photo-card">
            <img src="anh2.jpg" alt="Khoảnh khắc 2">
            <p class="photo-caption">📸 <b>Kỷ niệm 2:</b> Cảm ơn Ba vì đã luôn là chỗ dựa vững chắc cho cả gia đình.</p>
        </div>

        <div class="photo-card">
            <img src="anh3.jpg" alt="Khoảnh khắc 3">
            <p class="photo-caption">📸 <b>Kỷ niệm 3:</b> Những chuyến đi và kỷ niệm đẹp bên Ba luôn là điều tuyệt vời nhất.</p>
        </div>

        <div class="photo-card final-wish">
            <img src="anh4.jpg" alt="Ảnh gia đình">
            <div class="photo-caption">
                <h2 style="color: #ffd700; margin-bottom: 10px;">🎉 Happy Birthday Ba! 🎉</h2>
                <p>Chúc Ba bước sang tuổi mới luôn mạnh khỏe, gặp nhiều may mắn, vạn sự như ý và luôn hạnh phúc bên gia đình mình!</p>
                <br>
                <p style="text-align: center; font-weight: bold; color: #ff7675;">❤️ Con yêu Ba rất nhiều! ❤️</p>
            </div>
        </div>
    </div>

    <script>
        const music = document.getElementById('bg-music');
        const cards = document.querySelectorAll('.photo-card');
        let isPlayed = false;

        function checkScroll() {
            cards.forEach(card => {
                const rect = card.getBoundingClientRect();
                if (rect.top < window.innerHeight * 0.85) {
                    card.classList.add('active');
                    if (!isPlayed) {
                        music.play().catch(e => console.log(e));
                        isPlayed = true;
                    }
                }
            });
        }

        window.addEventListener('scroll', checkScroll);
        checkScroll();
    </script>
</body>
</html>
